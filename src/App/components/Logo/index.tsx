import { ReactElement } from "react";
import { Typography } from "@mui/material";

const Logo = (): ReactElement => {
  return <Typography variant="h5">Impulse.game</Typography>;
};
export default Logo;
